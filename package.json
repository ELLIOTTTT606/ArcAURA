{
  "name": "arcaura-refactored",
  "version": "1.0.0",
  "private": true,
  "description": "ArcAURA — plateforme de projection financière, planning et empreinte carbone (version refactorée).",
  "type": "module",
  "scripts": {
    "serve": "npx --yes http-server . -p 8080 -c-1 -o",
    "lint":  "npx --yes eslint \"js/**/*.js\"",
    "test":  "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
